# libdecnumber - macOS (aarch64-apple-darwin) config.h defines
#
# Identical to Linux — libdecnumber only needs endianness and standard headers.

CONFIG_STDC_HEADERS=1
CONFIG_HAVE_STRING_H=1
CONFIG_HAVE_STDLIB_H=1
