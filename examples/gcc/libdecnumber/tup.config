# libdecnumber - x86-64 Linux config.h defines
#
# libdecnumber includes "dconfig.h" which, when not IN_LIBGCC2,
# includes "config.h". We only need WORDS_BIGENDIAN (and standard headers).

CONFIG_STDC_HEADERS=1
CONFIG_HAVE_STRING_H=1
CONFIG_HAVE_STDLIB_H=1
