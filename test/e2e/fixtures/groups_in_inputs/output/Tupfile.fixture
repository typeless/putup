include_rules
# Group reference in REGULAR inputs (before |), not order-only section
# This is the spos pattern that was failing - %<json-headers> must expand
: $(ROOT)/modules/<json-headers> |> cat %<json-headers> > %o |> headers.txt
